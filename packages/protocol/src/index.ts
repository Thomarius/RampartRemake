// Wire message types and their runtime validators. See docs/PLAN.md section 6.
// Implemented in M4.

/** Bumped on any breaking change to the message set; clients on a different version are rejected. */
export const PROTOCOL_VERSION = 1;
